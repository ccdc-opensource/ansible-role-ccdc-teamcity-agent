#!/bin/bash
# chkconfig: 2345 95 05
# description: teamcity

case "$1" in
start|stop)
for a in "{{ teamcity_agent_install_dir }}"/agent??
do
su -l -c "$a/bin/agent.sh $1" buildman
done
;;
*)
echo "Usage: /etc/init.d/teamcity {start|stop}"
exit 1
;;
esac

exit 0
